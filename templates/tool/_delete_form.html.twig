<form method="post" action="{{ path('app_tool_delete', {'id': tool.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tool.id) }}">
    <button class="btn">Delete</button>
</form>

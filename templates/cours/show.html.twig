{% extends 'base.html.twig' %}

{% block title %}Cours{% endblock %}

           
{% block body %}
 <style>
* {
  box-sizing: border-box;
}

.zoom {
  padding: 50px;
  transition: transform .2s;
  width: 200px;
  height: 200px;
  margin: 0 auto;
}

.zoom:hover {
  -ms-transform: scale(1.5); /* IE 9 */
  -webkit-transform: scale(1.5); /* Safari 3-8 */
  transform: scale(1.5); 
}
</style>
    <h1>Cours</h1>
    <div class="container">
    <table class="table table-dark table-striped">
  
<div class="mt-5 mb-6 ">
    
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cour.id }}</td>
            </tr>
            <tr>
                <th>Nom_cours</th>
                <td>{{ cour.nomCours }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ cour.activite }}</td>
            </tr>
            <tr>
                <th>Date_cours</th>
                <td>{{ cour.dateCours ? cour.dateCours|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
            
                  <div class="zoom">
 <a href="/uploads/{{cour.image()}}" target="_blank"><img src="/uploads/{{cour.image()}}" alt="image_article" width="100" height="100" >
</div>
            </tr>
        </tbody>
        </div>
    </table>
    </div>


 





                                
    <div class="btn-icon-split">
    <a href="{{ path('app_cours_index') }}">back to list</a>
        </div>

    <a href="{{ path('app_cours_edit', {'id': cour.id}) }}">edit</a>

    {{ include('cours/_delete_form.html.twig') }}

{% endblock %}

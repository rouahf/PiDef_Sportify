<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Article</title>
    <style>
       :root {
    --primary-color: rgb(26, 150, 126);
}

h1 {
    color: var(--primary-color);
    margin-top: 50px;
    margin-bottom: 20px;
    font-size: 36px;
    text-align: center;
}

table {
    border: 2px solid var(--primary-color);
    background-color: rgba(0, 34, 255, 0.03);
    font-size: 20px;
    margin: 0 auto;
    width: 70%;
    border-collapse: collapse;
    margin-bottom: 50px;
}

th, td {
    border: 1px solid var(--primary-color);
    padding: 10px;
    text-align: left;
}
    </style>
<h1 class="align-center">Bienvenue dans votre application  </h1>

<p>Voila la liste de tout les articles</p>
<table class="table" border="5" >
    <tbody>
            {% for article in articles %}
            <tr>
                <th>Titre de l'Article</th>
                <td>{{ article.titreArticle }}</td>
            </tr>
             
            
            <tr>
                <th>Contenu de l'Article</th>
                <td>{{ article.contenuArticle }}</td>
            </tr>
            <tr>
                <th>Auteur de l'Article</th>
                <td>{{ article.auteurArticle }}</td>
            </tr>
              <tr>
                <th>date de l'Article</th>
            
             <td>{{article.getDateA()|date('d-M-y H:i:s')}}</td>
                </tr>
                <tr>
                <th>categorie</th>
            
             <td>{{article.getIdCategA().getTypeA }}</td>
                </tr>
        
                <tr>
                <th>Commentaires</th>
           
                   <td>{{ article.commentaires | length }} comments</td>

           </tr>
          <tr>
                <th>Likes</th>
           
                   <td>{{ article.likes | length }} likes</td>

           </tr>
          
           <td>image</td>

 <a href="/uploads/{{article.getImageArticle()}}" target="_blank"><img src="/uploads/{{article.getImageArticle()}}" alt="image_article" width="100" height="100" >
  
        </tbody>


    


</table>
 {% endfor %}